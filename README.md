- Insira seu token no arquivo json("configs.json") e depois use os seguintes comandos no cmd

```json
{
    "TOKEN" : "YOU TOKEN"
}
```

- Instala os pacotes necessarios

```cmd
pip.exe install -r "requirements.txt"
```
- Inicia o programa

```cmd
python.exe "src//main.py"
```
